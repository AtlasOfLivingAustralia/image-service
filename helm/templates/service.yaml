apiVersion: v1
kind: Service
metadata:
  name: svc-{{ include "ala-images.fullname" . }}
  labels:
    {{- include "ala-images.labels" . | nindent 4 }}
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-additional-resource-tags: {{ include "ala-images.resourceTags" . | quote }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort}}
      protocol: TCP
  selector:
    {{- include "ala-images.selectorLabels" . | nindent 4 }}

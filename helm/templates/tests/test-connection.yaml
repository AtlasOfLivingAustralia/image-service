apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ala-images.fullname" . }}-test-connection"
  labels:
    {{- include "ala-images.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ala-images.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
